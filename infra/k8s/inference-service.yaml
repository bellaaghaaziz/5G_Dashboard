apiVersion: v1
kind: Service
metadata:
  name: cellpilot-inference
  namespace: cellpilot-mlops
  labels:
    app: cellpilot-inference
spec:
  selector:
    app: cellpilot-inference
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
