{
  "name": "5g-platform-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "services/*",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n gateway,user,pred,dash,web \"npm run dev -w services/api-gateway\" \"npm run dev -w services/user-service\" \"npm run dev -w services/prediction-service\" \"npm run dev -w services/dashboard-service\" \"npm run dev -w apps/web\"",
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "devDependencies": {
    "@types/passport-jwt": "^4.0.1",
    "concurrently": "^9.2.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
